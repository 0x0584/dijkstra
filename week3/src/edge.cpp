#include "graph.hpp"

using namespace graph;
using namespace graph::impl;

// template <typename T, typename W>
// bool Graph<T, W>::Edge::residual(edge_ptr other) const {
//     return other->from() == to() and from() == other->to();
// }
